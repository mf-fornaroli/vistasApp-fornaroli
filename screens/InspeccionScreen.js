import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

const InspeccionScreen = () => {
 return (<View>
<Text>{'Inspeccion'}</Text>
 </View>)
}

const styles = StyleSheet.create({
 screen: {
  
 }
});

export default InspeccionScreen;